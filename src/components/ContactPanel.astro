---
import { siteConfig, withBasePath } from "../config";

const hasBooking = Boolean(siteConfig.bookingUrl);
const formAction = withBasePath("/gracias/");
const formSubmitUrl = withBasePath("/");
---

<section class="section contact-section" id="contacto" aria-labelledby="contact-title">
  <div class="container">
    <div class="contact-intro">
      <p class="eyebrow">Siguiente paso</p>
      <h2 id="contact-title">¿Cuántas horas costó esta semana la brecha entre tu operación y tus sistemas?</h2>
      <p class="lead">
        En 30 minutos revisamos el proceso que más fricción genera. En 48 horas recibes mi lectura por escrito, un rango o un “no soy la opción”.
      </p>
    </div>

    <ol class="next-steps" aria-label="Qué pasa después">
      <li><span>01</span><strong>Eliges ruta</strong><small>{hasBooking ? "Agenda o escribe. Dos minutos." : "Escribe el contexto. Dos minutos."}</small></li>
      <li><span>02</span><strong>Tres datos</strong><small>Nombre, email y contexto.</small></li>
      <li><span>03</span><strong>La llamada</strong><small>30 minutos sobre tu operación, conmigo.</small></li>
      <li><span>04</span><strong>En 48 horas</strong><small>Lectura + rango, o una respuesta honesta.</small></li>
    </ol>

    <div class:list={["contact-grid", !hasBooking && "contact-grid--single"]}>
      {hasBooking && (
        <article class="contact-card booking-card">
          <div>
            <p class="label">Ruta A · agenda</p>
            <h3>Reserva una llamada de encaje</h3>
            <p class="microcopy">El calendario se carga solo cuando lo pides; no compite con el rendimiento inicial.</p>
          </div>
          <button
            class="button"
            type="button"
            data-booking-open
            data-booking-url={siteConfig.bookingUrl}
            data-track="cta"
            data-location="booking"
            aria-describedby="booking-status"
          >
            Cargar agenda <span class="icon-arrow icon-arrow--down" aria-hidden="true"></span>
          </button>
          <p class="booking-status" id="booking-status" data-booking-status role="status" aria-live="polite"></p>
          <div class="booking-frame" data-booking-frame hidden></div>
          <p class="booking-direct">
            <a href={siteConfig.bookingUrl} target="_blank" rel="external noopener" data-track="cta" data-location="booking-direct">Abrir agenda directamente</a>
          </p>
          <noscript>
            <a href={siteConfig.bookingUrl} target="_blank" rel="external noopener">Abrir agenda en una pestaña nueva</a>
          </noscript>
        </article>
      )}

      <article class="contact-card contact-card--form">
        <div>
          <p class="label">{hasBooking ? "Ruta B · mensaje" : "Cuéntame el contexto"}</p>
          <h3>Prefiero escribir</h3>
          <p class="microcopy">Tres campos. Sin newsletter, sin secuencias automáticas.</p>
        </div>

        <form
          class="contact-form"
          name="contacto"
          method="POST"
          action={formAction}
          data-submit-url={formSubmitUrl}
          data-netlify="true"
          data-netlify-honeypot="bot-field"
          data-contact-form
        >
          <input type="hidden" name="form-name" value="contacto" />
          <input type="hidden" name="bot-field" />

          <label for="nombre">
            <span class="field-label">Nombre</span>
            <input id="nombre" name="nombre" type="text" autocomplete="name" required minlength="2" maxlength="80" aria-describedby="nombre-error" />
            <span class="field-error" id="nombre-error" data-error-for="nombre" aria-live="polite"></span>
          </label>

          <label for="email">
            <span class="field-label">Email de trabajo</span>
            <input id="email" name="email" type="email" autocomplete="email" inputmode="email" required maxlength="120" aria-describedby="email-error" />
            <span class="field-error" id="email-error" data-error-for="email" aria-live="polite"></span>
          </label>

          <label class="contact-form__wide" for="contexto">
            <span class="field-label">Una línea de contexto</span>
            <input
              id="contexto"
              name="contexto"
              type="text"
              required
              minlength="12"
              maxlength="280"
              aria-describedby="contexto-error"
              placeholder="Ej. conciliamos pedidos de cuatro sistemas a mano"
            />
            <span class="field-error" id="contexto-error" data-error-for="contexto" aria-live="polite"></span>
          </label>

          <div class="contact-form__actions contact-form__wide">
            <button class="button" type="submit" data-form-submit>Enviar contexto <span class="icon-arrow" aria-hidden="true"></span></button>
            <div>
              <p>Solo usaré estos datos para responderte.</p>
              <p class="form-status" data-form-status role="status" aria-live="polite"></p>
            </div>
          </div>
        </form>
      </article>
    </div>
  </div>
</section>

<style>
  .contact-section {
    border-top: var(--border-strong) solid var(--ink);
    background: var(--paper-raised);
  }

  .contact-intro {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(18rem, 0.75fr);
    gap: var(--space-7);
    align-items: end;
  }

  .contact-intro .eyebrow {
    grid-column: 1 / -1;
  }

  .contact-intro h2 {
    max-width: 21ch;
  }

  .next-steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--border);
    padding: 0;
    margin: var(--space-8) 0 var(--space-7);
    border: var(--border) solid var(--line);
    list-style: none;
  }

  .next-steps li {
    min-height: 9rem;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    padding: var(--space-5);
    border-right: var(--border) solid var(--line);
  }

  .next-steps li:last-child {
    border-right: 0;
  }

  .next-steps span,
  .next-steps strong {
    font-family: var(--font-data);
  }

  .next-steps span {
    color: var(--ink);
    font-size: var(--text-label);
    font-weight: 600;
  }

  .next-steps strong {
    font-size: var(--text-small);
    font-weight: 600;
  }

  .next-steps small {
    color: var(--graphite);
    font-size: var(--text-micro);
    line-height: 1.45;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 0.82fr 1.18fr;
    gap: var(--space-5);
    align-items: start;
  }

  .contact-grid--single {
    grid-template-columns: minmax(0, var(--reading));
  }

  .contact-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    padding: clamp(var(--space-5), 4vw, var(--space-7));
    border: var(--border) solid var(--line-strong);
    border-radius: var(--radius);
    background: var(--paper);
  }

  .contact-card--form {
    border: var(--border-strong) solid var(--ink);
    box-shadow: var(--shadow-offset) var(--shadow-offset) 0 var(--line);
  }

  .contact-card .label {
    margin-bottom: var(--space-3);
    color: var(--graphite);
  }

  .contact-card h3 {
    margin-bottom: var(--space-3);
  }

  .booking-card > .button {
    align-self: flex-start;
  }

  .booking-frame {
    min-height: 42rem;
    border-top: var(--border) solid var(--line);
  }

  .booking-frame :global(iframe) {
    width: 100%;
    min-height: 42rem;
    border: 0;
    background: var(--paper-raised);
  }

  .booking-direct {
    color: var(--graphite);
    font-size: var(--text-micro);
  }

  .booking-direct a {
    min-height: var(--target);
    display: inline-flex;
    align-items: center;
    color: var(--ink);
  }

  .booking-status,
  .form-status,
  .field-error {
    min-height: var(--space-5);
    color: var(--manual);
    font-size: var(--text-micro);
    line-height: 1.45;
  }

  .contact-form {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-5);
  }

  .contact-form label {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .contact-form .field-label {
    font-family: var(--font-data);
    font-size: var(--text-label);
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .contact-form input {
    width: 100%;
    min-height: var(--target);
    border: var(--border) solid var(--line-strong);
    border-radius: var(--radius);
    background: var(--paper-raised);
    color: var(--ink);
    padding: var(--space-3) var(--space-4);
  }

  .contact-form input::placeholder {
    color: var(--ink-muted);
  }

  .contact-form input:hover {
    border-color: var(--ink);
  }

  .contact-form input[aria-invalid="true"] {
    border-color: var(--manual);
    box-shadow: 0 0 0 var(--border) var(--manual);
  }

  .contact-form button[disabled] {
    cursor: wait;
    opacity: 0.72;
  }

  .contact-form__wide {
    grid-column: 1 / -1;
  }

  .contact-form__actions {
    display: flex;
    align-items: center;
    gap: var(--space-4);
  }

  .contact-form__actions p {
    color: var(--graphite);
    font-size: var(--text-micro);
  }

  @media (max-width: 56.25rem) {
    .contact-intro,
    .contact-grid {
      grid-template-columns: 1fr;
    }

    .contact-intro {
      gap: var(--space-5);
    }

    .next-steps {
      grid-template-columns: 1fr 1fr;
    }

    .next-steps li:nth-child(2) {
      border-right: 0;
    }

    .next-steps li:nth-child(-n + 2) {
      border-bottom: var(--border) solid var(--line);
    }
  }

  @media (max-width: 37.4375rem) {
    .next-steps,
    .contact-form {
      grid-template-columns: 1fr;
    }

    .next-steps li {
      min-height: auto;
      border-right: 0;
      border-bottom: var(--border) solid var(--line);
    }

    .next-steps li:last-child {
      border-bottom: 0;
    }

    .contact-form__wide {
      grid-column: auto;
    }

    .contact-form__actions {
      align-items: stretch;
      flex-direction: column;
    }

    .booking-card > .button {
      align-self: stretch;
    }
  }
</style>
